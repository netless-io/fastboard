import React from "react";
import { classNames } from "../helpers/utils";

export function ToolBox() {
  return <div className={classNames("tool-box", "left", classNames("placeholder"))}>tool-box</div>;
}
