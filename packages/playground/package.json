{
  "name": "playground",
  "private": true,
  "scripts": {
    "dev": "vite dev --host",
    "build": "vite build",
    "build:dev": "vite build --mode development"
  },
  "dependencies": {
    "clsx": "^1.1.1",
    "@netless/fastboard": "workspace:*"
  }
}
